[build]
builder = "DOCKERFILE"
dockerfilePath = "Containerfile"

[deploy]
preDeployCommand = "node src/scripts/migrate_database.ts"
healthcheckPath = "/health/ready"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
